import 'package:camera/camera.dart';

const String appName = "Roarx Scanner";

const defaultAnimationTime = Duration(milliseconds: 200);

const longAnimationTime = Duration(milliseconds: 1000);

const defaultDelayTime = Duration(milliseconds: 500);

List<CameraDescription> cameras = [];

String boxName = "create_files";
